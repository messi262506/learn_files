# 终端常用命令速查

---

## 一、文件与目录操作

### 1.1 目录导航

| 命令 | 作用 | 示例 |
|------|------|------|
| `pwd` | 显示当前所在目录路径 | `pwd` |
| `cd` | 切换目录 | `cd Documents` |
| `cd ..` | 回到上一级目录 | `cd ..` |
| `cd ~` | 回到用户主目录 | `cd ~` |
| `cd /` | 回到根目录 | `cd /` |

### 1.2 目录操作

| 命令 | 作用 | 示例 |
|------|------|------|
| `ls` | 列出当前目录内容 | `ls` |
| `ls -l` | 详细列表（含权限、大小、时间） | `ls -l` |
| `ls -a` | 显示所有文件（含隐藏文件） | `ls -a` |
| `ls -la` | 详细显示所有文件 | `ls -la` |
| `mkdir` | 创建新目录 | `mkdir 毕业设计` |
| `mkdir -p` | 创建多级目录 | `mkdir -p a/b/c` |
| `rmdir` | 删除空目录 | `rmdir 空文件夹` |
| `rm -rf` | 强制删除目录及所有内容 | `rm -rf 文件夹名` |

### 1.3 文件操作

| 命令 | 作用 | 示例 |
|------|------|------|
| `touch` | 创建空文件 | `touch README.md` |
| `cat` | 查看文件内容 | `cat 论文.docx` |
| `more` | 分页查看文件 | `more 长文件.txt` |
| `head` | 查看文件开头 | `head -n 10 文件.txt` |
| `tail` | 查看文件末尾 | `tail -n 10 文件.txt` |
| `cp` | 复制文件 | `cp 原文件 目标文件` |
| `mv` | 移动/重命名文件 | `mv 旧名 新名` |
| `rm` | 删除文件 | `rm 文件名.txt` |
| `rm -f` | 强制删除文件 | `rm -f 文件名.txt` |

---

## 二、Git 版本管理命令

### 2.1 基础配置

| 命令 | 作用 |
|------|------|
| `git config --global user.name "姓名"` | 设置用户名 |
| `git config --global user.email "邮箱"` | 设置邮箱 |
| `git config --global --list` | 查看全局配置 |

### 2.2 仓库操作

| 命令 | 作用 |
|------|------|
| `git init` | 初始化本地仓库 |
| `git clone <URL>` | 克隆远程仓库 |
| `git remote add origin <URL>` | 关联远程仓库 |
| `git remote -v` | 查看远程仓库地址 |

### 2.3 日常工作流程

| 命令 | 作用 |
|------|------|
| `git status` | 查看文件状态 |
| `git add .` | 暂存所有改动 |
| `git add 文件名` | 暂存指定文件 |
| `git commit -m "描述"` | 提交到本地仓库 |
| `git push` | 推送到远程仓库 |
| `git pull` | 拉取远程更新 |

### 2.4 历史记录

| 命令 | 作用 |
|------|------|
| `git log` | 查看完整提交历史 |
| `git log --oneline` | 简洁提交历史 |
| `git log --graph` | 图形化显示分支 |
| `git log --author="姓名"` | 查看指定作者提交 |

### 2.5 分支操作

| 命令 | 作用 |
|------|------|
| `git branch` | 查看分支列表 |
| `git branch 分支名` | 创建新分支 |
| `git checkout 分支名` | 切换分支 |
| `git checkout -b 分支名` | 创建并切换分支 |
| `git merge 分支名` | 合并指定分支 |
| `git branch -d 分支名` | 删除分支 |

### 2.6 撤销与回退

| 命令 | 作用 |
|------|------|
| `git checkout -- 文件名` | 撤销工作区修改 |
| `git reset HEAD 文件名` | 取消暂存 |
| `git restore 文件名` | 恢复文件到上次提交状态 |
| `git reset --hard <提交ID>` | 强制回退到指定版本 |
| `git revert <提交ID>` | 撤销指定提交（生成新提交） |

### 2.7 其他实用命令

| 命令 | 作用 |
|------|------|
| `git diff` | 查看未暂存的改动 |
| `git diff --cached` | 查看已暂存的改动 |
| `git stash` | 临时保存工作区 |
| `git stash pop` | 恢复临时保存的内容 |
| `git fetch` | 获取远程分支信息 |
| `git tag <标签名>` | 创建标签 |

---

## 三、文件搜索与查找

### 3.1 基础搜索

| 命令 | 作用 | 示例 |
|------|------|------|
| `find . -name "*.txt"` | 查找指定文件名 | `find . -name "*.md"` |
| `find . -type f` | 查找所有文件 | `find . -type f` |
| `grep "关键词" 文件` | 在文件中搜索 | `grep "TODO" *.md` |
| `grep -r "关键词"` | 递归搜索目录 | `grep -r "错误" .` |
| `grep -i "关键词"` | 忽略大小写搜索 | `grep -i "hello"` |

---

## 四、系统与网络

### 4.1 系统信息

| 命令 | 作用 |
|------|------|
| `whoami` | 显示当前用户名 |
| `date` | 显示当前日期时间 |
| `clear` | 清空终端屏幕 |
| `history` | 查看命令历史 |

### 4.2 网络操作

| 命令 | 作用 | 示例 |
|------|------|------|
| `ping <地址>` | 测试网络连接 | `ping github.com` |
| `curl <URL>` | 获取网页内容 | `curl https://www.baidu.com` |
| `wget <URL>` | 下载文件 | `wget https://example.com/file.zip` |

---

## 五、快捷键

### 5.1 基础快捷键

| 快捷键 | 作用 |
|--------|------|
| `Ctrl + C` | 终止当前命令 |
| `Ctrl + D` | 退出当前会话 |
| `Ctrl + L` | 清空屏幕（同 clear） |
| `Ctrl + R` | 搜索命令历史 |
| `Ctrl + A` | 跳转到行首 |
| `Ctrl + E` | 跳转到行尾 |
| `Ctrl + K` | 删除光标到行尾 |
| `Ctrl + U` | 删除光标到行首 |
| `Tab` | 自动补全命令/路径 |
| `↑ / ↓` | 上下切换历史命令 |

---

## 六、实用技巧

### 6.1 命令别名（可选）

在 `~/.bashrc` 或 `~/.zshrc` 中添加别名：

```bash
alias ll='ls -la'
alias gs='git status'
alias ga='git add'
alias gc='git commit -m'
alias gp='git push'
alias gl='git log --oneline'
```

### 6.2 常用组合命令

```bash
# 一次性完成提交推送
git add . && git commit -m "更新内容" && git push

# 查找包含特定内容的文件
grep -r "TODO" . --include="*.md"

# 创建目录并进入
mkdir new_folder && cd new_folder
```

---

## 七、注意事项

1. **谨慎使用 `rm -rf`**：该命令会强制删除文件/目录，无法恢复
2. **Git 操作前先 `git status`**：确保操作正确
3. **使用 Tab 补全**：输入命令/路径时按 Tab 键自动补全，避免拼写错误
4. **命令历史**：使用上下箭头或 `Ctrl+R` 快速调用历史命令

## 八、禁用笔记本自带键盘
```bash
# 禁用笔记本自带键盘
sc config i8042prt start= disabled
#  解除笔记本自带键盘禁用
sc config i8042prt start= auto
```